allowedFields: ['name', 'email', 'comment']
branch: 'master'
filename: entry{@timestamp} # The resolved path will be _data/posts/entry1470750784949.json
format: 'json'

generatedFields:
  myField:
    type: date
    options:
      format: timestamp
      
# moderation: true
